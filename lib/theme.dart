/// Persisted `ThemeMode` with Material 3 helpers and zero-restart
/// switching. Import this directly for theme-only usage.
library;

export 'src/theme/theme_controller.dart';
export 'src/theme/theme_scope.dart';
